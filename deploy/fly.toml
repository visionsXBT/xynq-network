app = "xynq-network"
primary_region = "fra"

[build]
  dockerfile = "deploy/Dockerfile"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  min_machines_running = 1

[[services]]
  internal_port = 8787
  protocol = "tcp"

  [[services.ports]]
    port = 8787
